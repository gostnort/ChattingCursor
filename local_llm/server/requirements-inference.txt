# local_llm 本地推理（llama.cpp GGUF sidecar）
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
huggingface-hub>=0.26.0
readability-lxml>=0.8.1

# llama-cpp-python 不在此列出：PyPI 无预编译 wheel，会触发源码编译失败。
# 请运行 local_llm/server/install.sh（Linux/macOS）或 install.bat（Windows），
# 脚本会按平台安装 CPU / CUDA / Metal 预编译 wheel。
